import * as React from 'react';

class Home extends React.Component {
    public render() {
        return (
            <div>
                Hello, this is todo app with react, redux, connect, async actions, router and so on
            </div>
        );
    }
}

export default Home;
